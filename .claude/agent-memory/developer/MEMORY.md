# Dave's Memory

## Environment

- Python 3 is available as `python3` (not `python`)
- No test framework installed; use `python3 -m unittest` for tests
- No package.json / Node tooling present — this is a minimal repo

## Repo Structure

- Minimal repo: only `README.md` at root initially
- No language or framework pre-configured
- Feature branches named `issue/<number>-<slug>` (already created by workflow)

## Conventions

- Conventional commits: `feat:`, `fix:`, `docs:`, `test:`, `refactor:`
- Do NOT push or create PRs — commit locally only
- Log entries go in `.claude/agent-memory/logs/YYYY-MM-DD.md`
